// Tương tự như JS, if else trong TS cũng hoạt động tương tự.

// if (condition) {
//     // code block
// } else {
//     // code block
// }

// Ví dụ:
let isShow: boolean = true;
if (isShow) {
    console.log('Show');
} else {
    console.log('Hide');
}